


// Populate saved events in associated textarea on load... cant figure this one out... 
// function onLoad() {
//     storedEvents.forEach(function(value) {
//         // successfully retrieve local storage time integer for each key value pair in local storage
//         let time  = value.time;
//         // push to time Arr
//         timeArr.push(time);
//         // successfully retrieve local storage event description for each key value pair in local storage 
//         let eventDescription = value.event;
//         // push to events Arr
//         eventsArr.push(eventDescription);
//     });
// };

        // $(".list-group").find(".hour").css({"color": "red", "border": "2px solid red"});
        // let textEx = $(".list-group").find(".hour");
        //     if (time)
        // textEx.next().text(event);
        // console.log(textEx.next());
        // hoursArray.push(textEx);
        // console.log(hoursArray);
            // for (i=0; i < hoursArray.length; i++) {
            //     if (hoursArray[i] === value.time + "AM" || value.time + "PM") {
            //         console.log(time);
            //         console.log(event);
            //     };
            // };
 


//     let $findHourEl = $(".hour");
//     console.log($findHourEl);
//     // this is returning object, i thought it was supposed to return an array of objects
//     console.log(typeof $findHourEl);
//     for (i=0; i < arrOfEvents.length; i++) {
//         // $(".list-group").find($findHourEl).css({"color": "red", "border": "2px solid red"});
//         let textEx = $(".list-group").find($findHourEl).text();
//         hoursArray.push(textEx);
//         console.log(hoursArray);
//         for (i=0; i < hoursArray.lengyth; i++
//             if (hoursArray[i] === value.time + "AM" || value.time + "PM") {

//             }

//     };
// };

    // console.log(textEx);

    // would like to add a .find() so only grab the div where its html/text is equal to the current iteration of the forEach loops 
    // value.time... see below... 

//     arrOfEvents.forEach(function(value) {
//         // successfully retrieve local storage time integer for each key value pair in local storage
//         let time  = value.time;
//         console.log(time);
//         // successfully retrieve local storage event description for each key value pair in local storage 
//         let event = value.event;
//         console.log(event);
//         //  let hour = parseInt(hourEl.text());
//         // console.log(hour);
//         // if (hour === time) {
//         //     console.log($(this).next());
//         // }
//     });
// };